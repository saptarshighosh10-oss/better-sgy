'use client'

// ThemeSync is intentionally empty — ThemeProvider owns all class application.
// The file is kept to avoid breaking any existing imports.
export function ThemeSync() {
  return null
}
